---
platform: linux

image: docker:///concourse/concourse-ci

inputs:
- name: git-bits-service-release
- name: git-bits-service

params:
  BOSH_TARGET:
  BOSH_USERNAME:
  BOSH_PASSWORD:

outputs:
- name: manifest

run:
  path: git-bits-service/ci/scripts/run.sh
