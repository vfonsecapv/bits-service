buildpacks:
  directory_key: 'buildpacks'
  fog_connection:
    provider: 'local'
    local_root: '/tmp/'
app_cache:
  directory_key: 'app_bits'
  fog_connection:
    provider: 'local'
    local_root: '/tmp/'
nginx:
  use_nginx: false
logging:
  file: /tmp/bits-service-test.log
  level: debug
