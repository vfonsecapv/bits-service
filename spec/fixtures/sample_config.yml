buildpacks:
  buildpack_directory_key: 'buildpacks'
  fog_connection:
    provider: 'local'
    local_root: '/tmp/'
nginx:
  use_nginx: false
logging:
  file: /tmp/bits-service-test.log
  level: debug
